# QooERP 前端应用 - 快速开始

## 🚀 快速启动

### Windows 用户
双击运行 `start.bat` 脚本,或在命令行执行:
```bash
cd packages\web
start.bat
```

### 手动启动
```bash
# 1. 安装依赖 (如果尚未安装)
cd d:/05workspaces/qooerp/qooerp-frontend
pnpm install

# 2. 启动开发服务器
cd packages/web
pnpm run dev
```

服务器将在 `http://localhost:3000` 启动

## 📝 首次使用

### 登录应用
1. 访问 `http://localhost:3000/login`
2. 输入任意用户名和密码
3. 点击登录按钮

### 访问主页
登录后会自动跳转到主页 (`/`)

## 🔧 开发环境配置

### 代理配置
默认代理到后端服务 `http://localhost:8080`

如需修改,编辑 `vite.config.ts`:
```typescript
server: {
  proxy: {
    '/api': {
      target: 'http://your-backend-url',
      changeOrigin: true,
      rewrite: (path) => path.replace(/^\/api/, ''),
    },
  },
}
```

## 📚 项目结构

```
packages/web/
├── src/
│   ├── api/           # API 接口
│   ├── assets/        # 静态资源
│   ├── components/    # 组件
│   ├── composables/   # 组合式函数
│   ├── config/        # 配置文件
│   ├── directives/    # 指令
│   ├── hooks/         # Hooks
│   ├── i18n/          # 国际化
│   ├── middleware/    # 中间件
│   ├── monitor/       # 监控
│   ├── router/        # 路由
│   ├── stores/        # 状态管理
│   ├── types/         # 类型定义
│   ├── utils/         # 工具函数
│   ├── views/         # 页面组件
│   ├── App.vue        # 根组件
│   └── main.ts        # 入口文件
├── public/            # 公共资源
└── index.html         # HTML 模板
```

## 🛠️ 技术栈

- **框架**: Vue 3 + TypeScript
- **构建工具**: Vite
- **UI 组件库**: TDesign Vue Next
- **状态管理**: Pinia
- **路由**: Vue Router
- **国际化**: Vue I18n
- **图表**: ECharts
- **HTTP 客户端**: Axios

## 🐛 故障排查

### 主页空白问题
参考 `DEBUG.md` 文件获取详细的调试步骤

### 常见问题
1. **依赖安装失败**: 删除 `node_modules` 和 `pnpm-lock.yaml`,重新安装
2. **端口被占用**: 修改 `vite.config.ts` 中的 `server.port`
3. **API 请求失败**: 检查后端服务是否启动
4. **TypeScript 错误**: 运行 `pnpm run type-check` 检查类型错误

## 📖 文档

- `DEBUG.md` - 调试指南
- `TROUBLESHOOTING.md` - 故障排查
- `README.md` - 项目说明

## 💡 提示

- 开发时使用 `pnpm` 包管理器
- 代码提交前运行 `pnpm run type-check`
- 遵循项目代码规范
- 定期更新依赖
