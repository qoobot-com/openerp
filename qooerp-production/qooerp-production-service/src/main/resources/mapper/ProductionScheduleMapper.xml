<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qoobot.qooerp.production.mapper.ProductionScheduleMapper">

    <select id="selectPageByCondition" resultType="com.qoobot.qooerp.production.entity.ProductionSchedule">
        SELECT * FROM production_schedule
        WHERE deleted = 0 AND tenant_id = #{tenantId}
        <if test="scheduleCode != null and scheduleCode != ''">
            AND schedule_code LIKE CONCAT('%', #{scheduleCode}, '%')
        </if>
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        <if test="equipmentId != null">
            AND equipment_id = #{equipmentId}
        </if>
        ORDER BY planned_start_time DESC
    </select>

</mapper>
