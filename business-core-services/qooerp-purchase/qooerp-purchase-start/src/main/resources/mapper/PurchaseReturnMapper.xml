<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qoobot.qooerp.purchase.mapper.PurchaseReturnMapper">

    <select id="queryPage" resultType="com.qoobot.qooerp.purchase.entity.PurchaseReturn">
        SELECT * FROM purchase_return
        <where>
            deleted = 0
            <if test="returnCode != null and returnCode != ''">
                AND return_code LIKE CONCAT('%', #{returnCode}, '%')
            </if>
            <if test="orderId != null">
                AND order_id = #{orderId}
            </if>
            <if test="supplierId != null">
                AND supplier_id = #{supplierId}
            </if>
            <if test="returnStatus != null and returnStatus != ''">
                AND return_status = #{returnStatus}
            </if>
            <if test="startDate != null">
                AND return_date &gt;= #{startDate}
            </if>
            <if test="endDate != null">
                AND return_date &lt;= #{endDate}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

</mapper>
