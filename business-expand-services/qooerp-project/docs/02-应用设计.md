# QooERP 项目管理模块 - 应用设计文档

> 模块名称: qooerp-project  
> 模块版本: v1.0  
> 创建日期: 2026-02-17  
> 作者: Auto  

---

## 一、应用架构

### 1.1 分层架构

```
Presentation Layer (Controller)
Business Layer (Service, Domain)
Data Access Layer (Mapper, Entity)
Infrastructure Layer (Database, Cache)
```

### 1.2 核心模块

| 模块 | 说明 |
|------|------|
| project | 项目管理 |
| task | 任务管理 |
| milestone | 里程碑管理 |
| member | 成员管理 |
| report | 报表管理 |

---

## 二、核心类设计

### 2.1 Project Service

```java
public interface ProjectService {
    ProjectDTO createProject(ProjectDTO dto);
    ProjectDTO updateProject(Long id, ProjectDTO dto);
    void approveProject(Long id);
    void closeProject(Long id);
    Page<ProjectDTO> listProjects(PageParam pageParam);
}
```

### 2.2 ProjectTask Service

```java
public interface ProjectTaskService {
    ProjectTaskDTO createTask(ProjectTaskDTO dto);
    void assignTask(Long id, Long userId);
    void completeTask(Long id);
    List<ProjectTaskDTO> getProjectTasks(Long projectId);
}
```

---

## 三、接口设计

### 3.1 Controller

```java
@RestController
@RequestMapping("/api/project/projects")
public class ProjectController {
    @PostMapping
    public Result<ProjectDTO> createProject(@RequestBody ProjectDTO dto);
    
    @PutMapping("/{id}")
    public Result<ProjectDTO> updateProject(@PathVariable Long id, 
        @RequestBody ProjectDTO dto);
    
    @PostMapping("/{id}/approve")
    public Result<Void> approveProject(@PathVariable Long id);
}
```

---

## 四、配置设计

```yaml
spring:
  application:
    name: qooerp-project
  datasource:
    url: jdbc:postgresql://localhost:5432/qooerp_project
```

---

**文档版本**: v1.0  
**最后更新**: 2026-02-17
